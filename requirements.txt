# Spotify Data Pipeline - Python Dependencies
# Install with: pip install -r requirements.txt

# Data Pipeline Core
dbt-core>=1.7.0
dbt-postgres>=1.7.0

# Note: dbt-expectations and dbt-utils are dbt packages, not pip packages
# They are installed via `dbt deps` from packages.yml

# Kaggle Dataset Download
kagglehub>=0.2.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Apache Airflow - runs in Docker container, not needed locally
# Airflow doesn't support Python 3.14 yet
# apache-airflow==2.8.0
# apache-airflow-providers-postgres>=5.0.0

# Testing & Quality
pytest>=7.0.0
pytest-cov>=4.0.0
sqlfluff>=2.3.0
yamllint>=1.32.0
pre-commit>=3.0.0

# Type Checking & Linting
mypy>=1.0.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
click>=8.0.0
rich>=13.0.0

# Jupyter Notebooks & Visualization
jupyter>=1.0.0
notebook>=7.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
